name: blockapps-privacy
ghc-options: -Wall -Werror

dependencies:
  - base
  - blockapps-data
  - blockapps-util
  - containers
  - format
  - lens
  - prometheus-client
  - QuickCheck
  - resourcet
  - strato-model
  - text
  - transformers

library:
  source-dirs: src/

tests:
  privacy-test:
    main: Main.hs
    source-dirs: test/
    dependencies:
      - blockapps-privacy
      - hspec
      - hspec-expectations-lifted
