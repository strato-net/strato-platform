pipeline {
    agent {
        label "multinode-102"
    }

    stages {
        stage('Init') {
            echo "init"
        }

        stage('Test') {
            echo "test"
        }

        stage('Record') {
            echo "record"
        }

    }
}