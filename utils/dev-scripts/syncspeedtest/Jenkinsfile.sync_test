pipeline {
    agent {
        label "multinode-102"
    }

    stages {
        stage('Init') {
            steps {
                echo "init"
            }

        }

        stage('Test') {
            steps {
                echo "test"
            }
        }

        stage('Record') {
            steps {
                echo "record"
            }
        }

    }
}