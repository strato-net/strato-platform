{
  "name": "strato-edge-canary",
  "version": "0.1.0",
  "private": true,
  "description": "CloudWatch Synthetics canary for STRATO: edge health, block age, and a signed no-op transaction timed to inclusion. Install with `npm ci --omit=dev` here before `cdk synth`; the handler files live in node_modules/ by Synthetics convention.",
  "dependencies": {
    "@noble/curves": "^1.9.0",
    "@noble/hashes": "^1.8.0"
  }
}
