name:                sql-monad
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src
  dependencies:
  - blockapps-data
  - mtl
  - persistent
  - postgresql-simple
  - resource-pool