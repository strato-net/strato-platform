contract SampleEvent {
    enum SampleEventEnum {
        NULL,
        PLAN,
        DRILL,
        SPLIT,
        SHIP,
        DESTROY,
        DEPLETE,
        ACK,
        ANALYZE,
        STORE
    }
}
