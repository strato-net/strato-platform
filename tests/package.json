{
  "name": "silo-test",
  "version": "0.0.1",
  "description": "platform tests",
  "scripts": {
    "test": "npm run test:blockhash && npm run test:e2e && npm run test:dataTypes && npm run test:load && npm run test:load10k && npm run test:loadecho && npm run test:solidvm && npm run test:upload && npm run test:upload10k && npm run test:loadFF && npm run test:truchainz && npm run test:multicontract && npm run test:uploadOrder && npm run test:slipstream",
    "test:blockhash": "mocha -r ts-node/register e2e/blockhash.test.ts",
    "test:e2e": "mocha -r ts-node/register e2e/createUser.test.ts && mocha -r ts-node/register e2e/import.constructor.test.ts && mocha -r ts-node/register e2e/import.test.ts",
    "test:dataTypes": "mocha -r ts-node/register regressions/address.test.ts && mocha regressions/bool.test.js && mocha regressions/bytes.test.js && mocha regressions/enum.test.js && mocha regressions/string.test.js && mocha regressions/uint.test.js && mocha regressions/negative.test.js",
    "test:load": "mocha load/stratoLoad.test.js --batchCount 20 --batchSize 20 && mocha load/stratoLoadFactory.test.js --batchCount 20 --batchSize 20",
    "test:load10k": "mocha load/stratoLoad.test.js --batchCount 100 --batchSize 100",
    "test:loadecho": "mocha load/stratoLoadEcho.test.js --batchCount 20 --batchSize 20 --config config/localhost.config.yaml",
    "test:solidvm": "mocha -r ts-node/register e2e/solidvm.test.ts",
    "test:upload": "mocha -r ts-node/register load/uploadString.test.ts --batchCount 10 --batchSize 10 && mocha load/uploadUniqueAddresses.test.js --batchCount 10 --batchSize 10",
    "test:upload10k": "mocha load/uploadString.test.js --batchCount 100 --batchSize 100",
    "test:loadFF": "mocha load/stratoLoadFiscalFactory.test.js --batchCount 100 --batchSize 100",
    "test:loadFFEvents": "mocha load/stratoLoadFiscalFactoryEvent.test.js --batchCount 100 --batchSize 100",
    "test:truchainz": "mocha -r ts-node/register e2e/createChain.test.ts && mocha -r ts-node/register e2e/governance.test.ts",
    "test:multicontract": "mocha -r ts-node/register e2e/multicontract.test.ts",
    "test:uploadOrder": "mocha -r ts-node/register e2e/uploadOrder.test.ts",
    "test:slipstream": "mocha -r ts-node/register e2e/slipstream.test.ts"
  },
  "dependencies": {
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.14.6",
    "bignumber.js": "^9.0.0",
    "blockapps-rest": "../blockapps-rest",
    "chai": "^4.0.2",
    "chai-bignumber": "^3.0.0",
    "co": "^4.6.0",
    "co-mocha": "^1.2.0",
    "enum": "^2.5.0",
    "json-to-csv": "^1.0.0",
    "mocha": "^3.4.2",
    "moment": "^2.22.2",
    "request": "^2.88.0",
    "request-promise": "4.2.2",
    "ts-node": "^9.0.0"
  }
}
