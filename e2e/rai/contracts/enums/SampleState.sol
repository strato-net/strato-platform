contract SampleState {
    enum SampleStateEnum {
        NULL,
        START,
        PLANNED,
        COLLECTED,
        SHIPPED,
        RECEIVED,
        ANALYZED,
        STORED,
        END
    }
}
