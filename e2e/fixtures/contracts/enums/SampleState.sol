contract SampleState {
    enum SampleStateEnum {
        NULL,
        START,
        PLANNED,
        COLLECTED,
        COLLECTED_SPLIT,
        SHIPPED,
        RECEIVED,
        ANALYZED,
        RECEIVED_SPLIT,
        STORED,
        SPLIT,
        DESTROYED,
        END
    }
}
