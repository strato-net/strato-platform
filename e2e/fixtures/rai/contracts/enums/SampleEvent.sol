contract SampleEvent {
    enum SampleEventEnum {
        NULL,
        PLAN,
        DRILL,
        SHIP,
        ACK,
        ATTACH_REPORT,
        STORE,
        SPLIT_RECEIVED,
        DESTROY,
        SPLIT_COLLECTED
    }
}
