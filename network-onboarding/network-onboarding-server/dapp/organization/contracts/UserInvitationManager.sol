pragma solidvm 3.0;

/**
 * UserInvitationManager allows organizations to invite users into their organization. When
 * an unknown user logs in, the NOA will check to see if the user has an invitation in their name.
 * If so, the app will create their blockchain user account and post their pre-created x509 
 * certificate.
 */
contract UserInvitationManager {
  // TODO
}