name: blockapps-init
license: Apache-2.0

dependencies:
  - base

library:
  source-dirs: src
  dependencies:
  - composable-monads-base
  - conduit
  - cross-monitoring
  - prometheus-client
  - text
  - transformers
  - unix

executables:
  test-reload:
    source-dirs: exec_src/
    main: TestReload.hs
    other-modules: []
    dependencies:
      - blockapps-init
