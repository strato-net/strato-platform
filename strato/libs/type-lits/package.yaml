name:                type-lits
license:             Apache-2.0

library:
  source-dirs: src
  dependencies:
    - base
    - aeson >=2.1.1.0 && <2.3
    - bifunctors
    - comonad
    - QuickCheck
    - text
