name:                streaming
version:             0.1.0.0
license:             Apache-2.0

# Umbrella package for message streaming.
# Active backend: streaming-kafka-hw

dependencies:
- base >= 4.7 && < 5
- kafka-monad
- streaming-kafka-hw

library:
  source-dirs: src
  exposed-modules:
  - Control.Monad.Composable.Streaming
  - Control.Monad.Composable.Streaming.DockerConfig
