name:                vault-monad
version:             0.1.0.0
license:             Apache-2.0

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src
  dependencies:
  - blockapps-vault-wrapper-api
  - blockapps-vault-wrapper-client
  - mtl
  - strato-auth
  - strato-model
