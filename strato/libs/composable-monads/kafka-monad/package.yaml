name:                kafka-monad
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src
  dependencies:
  - composable-monads-base
  - milena
  - milena-tools
  - mtl
  - transformers
  - unliftio-core
  
  
