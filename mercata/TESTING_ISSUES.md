# Testing Issues

| ID | Status | Area | Wallet Context | Issue | Repro | Expected | Actual | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BRIDGE-001 | done | Bridge In | STRATO authenticated | STRATO Wallet appears as a connect option in the bridge-in external wallet modal. | Log in with STRATO Wallet, open Bridge In, click the external wallet connect control. | The modal should offer only usable external wallets for bridge-in funding. STRATO Wallet should be hidden because the user is already app-authenticated with STRATO and cannot bridge in from the STRATO wallet. | The bridge external wallet control hides STRATO Wallet and only exposes external wallet options. | Confirmed fixed on 2026-05-04. |
| BRIDGE-002 | open | Deposits page | STRATO authenticated, after user/address switch | Deposit history does not invalidate or rescope old logged-in user data. | Log in or bridge with one STRATO user, switch/login as another STRATO user, then open Deposits. | Deposits should be scoped to the current STRATO user or stale local pending deposits should be invalidated when the authenticated STRATO identity changes. | Deposits page can show old-user pending deposits alongside current-user deposits; screenshot shows same external sender with different STRATO recipients (`0x3eD1...9D0e` and `0x69a0...f925`). | Likely pending deposit cache/query scoping issue. Log only. |
| BRIDGE-003 | open | Bridge In / Recent Transactions | MetaMask external wallet, STRATO auth transitions | New bridge-in flow can appear stuck even though Recent Transactions shows the deposit completed. | Bridge in with MetaMask, then inspect Fund/Recent Transactions and Deposits after auth/user changes. | Bridge progress state, recent transactions, and deposits page should converge on one completed/pending status for the same external tx/current recipient. | Screenshots show Recent Transactions reporting completed deposits (`100 USDC`, `10 USDC`) while the newer flow can remain stuck or show stale recipient context. | Needs correlation of local `pendingDeposits`, authenticated STRATO recipient, external sender, and backend transaction status. |
| BRIDGE-004 | open | Bridge In balance display | MetaMask external wallet on Ethereum Sepolia | Bridge-in eligible/sendable USDC balance can materially differ from the wallet's displayed USDC balance. | Connect MetaMask, select Bridge In from Ethereum Sepolia, choose USDC, and compare the app's `Balance:` under You Send with MetaMask's USDC balance for the same account/network. | The bridge-in max/eligible balance should match the wallet's source-chain token balance. | Screenshot from 2026-05-04 shows app `Balance: 24.99999 USDC` for external wallet `0x3eD1...9D0e`; tester reports the wallet balance is a very different number, not just a rounding delta. | Code path reads ERC20 `balanceOf(externalSender)` for the selected external token and formats with `externalDecimals`; investigate wrong source token address, wrong source network, stale external sender, or decimals mismatch. |
| BRIDGE-005 | done | Bridge Out | MetaMask external wallet and/or STRATO authenticated | Bridge-out needs to support both current STRATO Wallet behavior and MetaMask end-to-end STRATO -> Ethereum withdrawal signing. | Attempt Bridge Out with MetaMask-only on STRATO/Helium to Ethereum Sepolia, and also with STRATO Wallet authenticated plus MetaMask as recipient. | MetaMask-only Bridge Out should prepare unsigned STRATO withdrawal transactions, prompt MetaMask to add/switch to STRATO if needed, sign the STRATO-side approval/withdrawal, and release to the selected external-chain recipient. STRATO Wallet authenticated Bridge Out should continue using the existing STRATO session path. | Backend withdrawal route allows wallet-auth unsigned transaction flow, Bridge Out submit explicitly opts into wallet auth only when the app is not STRATO-authenticated, and nginx wallet-auth/CSRF allowlists include `/api/bridge/requestWithdrawal`. | Confirmed fixed on 2026-05-04 for MetaMask end-to-end and STRATO Wallet paths. |
| AUTH-001 | done | Logout | STRATO authenticated + external wallet connected | Logging out of STRATO leaves the external wallet connected, so the app stays in an external-wallet logged-in state. | Log in with STRATO, connect MetaMask for bridge-in, then use STRATO account menu logout. | Logout should terminate both identities: STRATO session and external wallet connection. | Logout terminates both STRATO session and external wallet state. | Confirmed fixed on 2026-05-04. |
| WALLET-001 | done | MetaMask chain registration | MetaMask external wallet | Mutating paths can fail when MetaMask is asked to switch to the STRATO chain before the chain has been added to the wallet. | Use a MetaMask account that does not already have the local/test STRATO chain configured, then trigger a flow that switches to STRATO. Exact newly reported flow TBD. | The app should either call `wallet_addEthereumChain` before switching or avoid routing MetaMask through STRATO-only signing paths. | Shared STRATO chain ensure path now adds the chain on MetaMask `4902` and then switches; Swap confirmed working on 2026-05-04. | Fixed via shared `ensureStratoChainInWallet` path used by unsigned-tx signing and Transfer. |
| WALLET-002 | open | MetaMask token import | MetaMask on STRATO network | Default token import/display can use the wrong decimals for STRATOETH. | Add/import STRATOETH into MetaMask on the STRATO network and compare against the app Portfolio/My Deposits balances. | MetaMask should display the same decimal-scaled token amount as the app. | App shows STRATOETH around `0.0272`, while MetaMask displays `27,273.08T STRATOETH`, indicating token decimals/metadata are wrong in the wallet import path. | Screenshot from 2026-05-04 shows USDST roughly aligned but STRATOETH badly inflated. Log only. |
| WALLET-003 | done | MetaMask network onboarding | MetaMask external wallet | STRATO network may not persist in MetaMask until a transfer/transaction flow forces it in. | Use a MetaMask profile without STRATO configured, connect to the app, then try non-transfer mutating flows such as Swap before doing a Transfer. | Connecting or entering any MetaMask-backed STRATO mutating flow should ensure the STRATO network is added and available in MetaMask. | Swap now triggers MetaMask network add/switch without requiring Transfer first. | Confirmed fixed on 2026-05-04 after shared STRATO chain ensure path landed. |
| EARN-001 | done | ETH Carry Vault | MetaMask connected on buildtest | Carry Vault deposit fails when app attempts to switch MetaMask to an unrecognized STRATO chain. | On buildtest, connect MetaMask, bridge in ETH, then attempt to deposit into ETH Carry Vault. | Deposit flow should either ensure the STRATO chain is added to MetaMask before switching or avoid a MetaMask chain switch path that cannot succeed. | Shared unsigned-tx signing path now ensures the STRATO chain is added before switching, covering Carry Vault deposit. | Closed on 2026-05-04 as same root fix as `WALLET-001`; direct Carry Vault retest still useful if later failures appear. |
