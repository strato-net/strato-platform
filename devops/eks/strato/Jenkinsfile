pipeline {
    agent { label "test3" }
    stages {
        stage('Stage 1') {
            steps {
                sh 'whoami'
                sh 'kubectl version --short --client'
            }
        }
    }
}
