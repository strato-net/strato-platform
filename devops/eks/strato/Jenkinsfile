pipeline {
    agent test3
    stages {
        stage('Stage 1') {
            steps {
                AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION="us-east-1" kubectl get pods -n strato-platform
            }
        }
    }
}
