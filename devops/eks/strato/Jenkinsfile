pipeline {
    agent { label "test3" }
    stages {
        stage('Stage 1') {
            steps {
                sh 'whoami'
                sh 'ls -l /home/ubuntu/bin/kubectl'
                sh '/home/ubuntu/bin/kubectl version --short --client'
            }
        }
    }
}
